{
  "name": "root",
  "private": true,
  "workspaces": [
    "auto-nested-routes/*",
    "nested-routes/*",
    "parcel/*",
    "sandbox/*",
    "switcher/*"
  ],
  "devEngines": {
    "node": ">=12.22"
  },
  "scripts": {
    "run:parcel": "lerna run start --scope @case-parcel/**",
    "run:switcher": "lerna run start --scope @case-switcher/**",
    "run:sandbox": "lerna run start --scope @case-sandbox/**",
    "run:nested-routes": "lerna run start --scope @case-nested-routes/**",
    "run:auto-nested-routes": "lerna run start --scope @case-auto-nested-routes/**"
  },
  "dependencies": {
    "@versea/core": "0.2.3",
    "@versea/plugin-sandbox": "0.2.3",
    "@versea/plugin-source-entry": "0.2.3",
    "@versea/plugin-auto-wait-container": "0.2.3",
    "@versea/shared": "0.2.3",
    "@versea/tapable": "0.2.3",
    "@versea/versea": "0.2.3",
    "@vue/cli": "^5.0.4",
    "create-react-app": "^5.0.0",
    "eslint-config-react-app": "^7.0.1",
    "lerna": "^4.0.0",
    "inversify": "^6.0.1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/versea/versea-examples.git"
  }
}
